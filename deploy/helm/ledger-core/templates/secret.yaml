apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ledger-core.fullname" . }}-secret
  labels:
    {{- include "ledger-core.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- include "ledger-core.secretData" . | nindent 2 }}
